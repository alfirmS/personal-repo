# ---
# apiVersion: v1
# kind: PersistentVolume
# metadata:
#   name: nginx-pv
# spec:
#   capacity:
#     storage: 1Gi
#   accessModes:
#     - ReadWriteOnce
#   hostPath:
#     path: /apps/nginx
# ---
# apiVersion: v1
# kind: PersistentVolumeClaim
# metadata:
#   name: nginx-pvc
# spec:
#   accessModes:
#     - ReadWriteOnce
#   resources:
#     requests:
#       storage: 1Gi
#   selector:
#     matchLabels:
#       app: nginx
---
# Source: porto-nginx/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: porto-nginx
  labels:
    name: porto-nginx
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    name: porto-nginx
---
# Source: porto-nginx/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: porto-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      name: porto-nginx
  template:
    metadata:
      labels:
        name: porto-nginx
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: porto-nginx
          image: index.docker.io/sfirman87/porto_nginx:v1.6
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          resources:
            limits:
              memory: "512Mi"
              cpu: "0.5"
            requests:
              memory: "256Mi"
              cpu: "0.1"
          # volumeMounts:
          #   - name: nginx-pv
          #     mountPath: /etc/nginx
          #   - name: nginx-pv
          #     mountPath: /var/log/nginx
          #   - name: nginx-pv
          #     mountPath: /var/www/html
---
# Source: porto/templates/hpa.yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: porto-nginx
  labels:
    name: porto-nginx
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: porto-nginx
  minReplicas: 1
  maxReplicas: 2
  metrics:
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
